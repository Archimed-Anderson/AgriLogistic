# ==============================================================================
# Kong Gateway environment (example)
# Copy to `.env.kong` (do not commit) and fill values.
#
# Note: files starting with `.env` may be ignored/filtered by tooling, so this
# example is intentionally named `env.kong.example`.
# ==============================================================================

# Kong database
KONG_PG_USER=kong
KONG_PG_DATABASE=kong
KONG_PG_PASSWORD=change_me_generate_secure

# Grafana (optional profile: monitoring)
GRAFANA_USER=admin
GRAFANA_PASSWORD=change_me_generate_secure

