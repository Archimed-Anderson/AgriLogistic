apiVersion: 1

providers:
  - name: 'AgroLogistic Dashboards'
    orgId: 1
    folder: 'AgroLogistic'
    folderUid: 'AgroLogistic'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
